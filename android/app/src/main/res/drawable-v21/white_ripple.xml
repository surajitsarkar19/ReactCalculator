<?xml version="1.0" encoding="utf-8"?>
<!--<ripple
    android:color="@color/lock"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/mask"
        android:drawable="@color/screenBackground" />

</ripple>-->

<ripple
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/focusable">

    <!-- Note: <ripple> acts like a layer-list -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <!-- This color is not displayed in any way -->
            <solid android:color="@color/focusable" />
        </shape>
    </item>

    <item>
        <color android:color="@color/buton_text" />
    </item>
    <!-- This is the border -->
    <!--<item>
        <shape android:shape="rectangle">
            <corners android:radius="3dp"/>
            &lt;!&ndash; Use your border color in place of #f00 &ndash;&gt;
            <stroke android:width="1dp" android:color="@color/lock_ripple"/>
        </shape>
    </item>-->
</ripple>